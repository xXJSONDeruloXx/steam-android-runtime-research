# AYN Thor rootless R38 stable payload replay — bsdtar retry predeclaration — 2026-08-12

Run identity: `thor-rootless-r38-stable-payload-replay-retry-20260812T003713Z`;
sub-run: `R38-stable-payload-replay-retry-bsdtar-extraction`.

Status: predeclared as a fresh retry of doc 507 after the invalid preparation
attempt recorded in doc 508. The first attempt never launched Steam. This retry
does not introduce a new Steam/client variable; it corrects only the archive
reader needed to materialize the already-declared Holo rootless fixture.

## Corrected infrastructure boundary

Android toybox `tar` rejected symlink and escaped systemd names in the pinned
Holo `system.rootfs.zst`. Use the existing app-owned ARM64 Holo `bsdtar`
bootstrap generated by `android/nova-lab/build-bsdtar-bootstrap.sh`:

```text
bootstrap_manifest_sha256=8d9befcdbaa1cb4aa918677d021f0cfe63185accc1f85d5c9e59a7ea8008dab6
usr/bin/bsdtar_sha256=0cf2ec3c3ec5c23c59eb7b755fb6ca2e0393aa82f75aba6959cffeb32086d7d6
bootstrap_files=18
```

The extraction helper must receive, in addition to the unchanged R38 inputs:

```text
NOVA_ROOTLESS_BOOTSTRAP_ROOTFS=/data/user/0/com.xjsonderulo.steamandroid.novalab/files/r38/input/nova-bsdtar-bootstrap
NOVA_ROOTLESS_PROOT_BIN=/data/user/0/com.xjsonderulo.steamandroid.novalab/files/r38/input/proot/proot
NOVA_ROOTLESS_PROOT_LOADER=/data/user/0/com.xjsonderulo.steamandroid.novalab/files/r38/input/proot/loader
NOVA_ROOTLESS_PROOT_LIB_DIR=/data/user/0/com.xjsonderulo.steamandroid.novalab/files/r38/input/proot/lib
```

The bootstrap is a provisioning implementation detail, not a Steam runtime,
client, graphics, network, D-Bus, or presentation change. Do not use the
preserved rooted Holo tree as the bootstrap source; transfer the pinned
app-owned bootstrap input and record its manifest before extraction.

## Fixed R38 experiment

Keep the exact verified stable endpoint and native hashes from doc 507:

```text
manifest_sha256=a2ad912ef6f150d373504a80c79f95210f8ed4ddbc42071593d0a120eb96ca91
payload_sha256=38dad8316435b1aea099f16c4eefb5da1fd57faa59fafe659945879ad5ae5148
decoded_zip_sha256=aed451902b2239cbf8a16e1a2004e016cea803d562849a4969b01a93c9cb4821
steam=72f48fb9c3f2c19cf64f8f7bbf4c7b7af65a73571ae0eeaab05bffd1633f4126
steamui.so=25ad66cfc78590b1745301ae7b86b70db64f5be0cd142d33134796e203fc8b76
vgui2_s.so=705f45328bb03c42509d28b748b0b499938f5e0caf2db618e8c929f0c3044186
steamwebhelper=3176d90436e49f7d34524ca2686cd324e2583806ba78841852b189fbcee0b83c
```

Use Thor serial `d234a848`, API 33, ARM64, app UID 10138, fresh app-owned
state, direct TCP Termux:X11 on `127.0.0.1:77`, inherited Android network,
the R38 short temporary paths, and the exact guest command/environment from
doc 507. Keep both Vulkan selectors and all loader/Mesa/software overrides
unset. Do not add `/dev/shm`, D-Bus, machine-id, Runtime 4, Proton, FEX/DXVK,
Gamescope, AHardwareBuffer, controller/audio helpers, or SteamUI patches.

Reuse only the verified immutable public stable tree, Holo archive/package
closure, PRoot files, and bootstrap files. Recreate R38 state, resolver,
client staging, X11 process, sockets, logs, screenshots, and readiness from
fresh scopes. The exact scopes are reused only after doc 508 proved them
absent:

```text
/data/local/tmp/thor-rootless-r38-stable-payload-replay-20260812T001312Z/
/data/user/0/com.xjsonderulo.steamandroid.novalab/files/r38/
/data/data/com.termux/files/home/.nova-rootless/
```

## Acceptance and classification

First require app-UID `nova_rootless_rootfs_archive=pass`, package-closure
pass, rootless preflight pass, fresh X11 handshake, and all four stable native
hashes after transfer. If any of those gates fail, classify the retry as
invalid infrastructure and stop before Steam.

If provisioning passes, launch the normal R38 updater command and capture
fresh Steam/updater/SteamUI/webhelper/X11/process/listener/screenshot evidence.
Then classify only the first native boundary:

1. stable hashes or device/UID/scope mismatch: `invalid`;
2. stable payload launches but `vgui2_s` still fails:
   `stable-client-loader-boundary-remains-open`;
3. `vgui2_s` is absent and native SteamUI/webhelper starts: record that
   milestone, then classify Vulkan, `/dev/shm`, D-Bus, X11, and presentation
   independently.

Do not use a black X11 canvas as a Steam frame and do not advance a second
prerequisite in this retry.

## Cleanup and authentication boundary

After evidence capture, terminate only the fresh retry X11 PID and sessions,
restore Termux properties byte-for-byte if temporarily changed, remove only
the exact R38 scopes, verify no Steam/PRoot/webhelper/X11 process or 6077
listener remains, and verify both rooted rollback paths.

No Steam authentication secret, session token, cookie, QR state, machine-auth
file, authenticated Steam home, or `steam.token` contents may be read, copied,
backed up, committed, or exported.
