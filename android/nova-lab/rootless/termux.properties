# Required by the Termux RUN_COMMAND contract for a Nova-owned launcher.
# The Nova APK must still request com.termux.permission.RUN_COMMAND and the
# user must grant it in Android settings before the rootless profile is active.
allow-external-apps=true
